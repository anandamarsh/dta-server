{
  "name": "DTA-Server",
  "version": "0.0.1",
  "scripts": {
    "preinstall": "npm install pm2 -g",
    "start": "pm2 start -f --attach ./app.js --watch"
  },
  "engines": {
    "node": "=8.9.4"
  },
  "dependencies": {
    "express": "4.16.2"
  }
}
